

#include <stdint.h>
#include <stdio.h>
#include <assert.h>

void state_write( uint32_t *loc, uint32_t val)
{
    assert(0);
}

void state_write_p (uint32_t **ploc, uint32_t *pval)
{
    assert(0);
}
